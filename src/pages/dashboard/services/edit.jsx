import { CONFIG } from 'src/global-config';

// TODO: Implementasi halaman services edit

const metadata = { title: `services edit - ${CONFIG.appName}` };

export default function Page() {
  return (
    <>
      <title>{metadata.title}</title>
      <div>Halaman services edit — dalam pengembangan</div>
    </>
  );
}
