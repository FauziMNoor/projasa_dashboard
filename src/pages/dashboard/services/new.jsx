import { CONFIG } from 'src/global-config';

// TODO: Implementasi halaman services new

const metadata = { title: `services new - ${CONFIG.appName}` };

export default function Page() {
  return (
    <>
      <title>{metadata.title}</title>
      <div>Halaman services new — dalam pengembangan</div>
    </>
  );
}
