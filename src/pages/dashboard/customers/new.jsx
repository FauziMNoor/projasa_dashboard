import { CONFIG } from 'src/global-config';

// TODO: Implementasi halaman customers new

const metadata = { title: `customers new - ${CONFIG.appName}` };

export default function Page() {
  return (
    <>
      <title>{metadata.title}</title>
      <div>Halaman customers new — dalam pengembangan</div>
    </>
  );
}
