import { CONFIG } from 'src/global-config';

// TODO: Implementasi halaman customers edit

const metadata = { title: `customers edit - ${CONFIG.appName}` };

export default function Page() {
  return (
    <>
      <title>{metadata.title}</title>
      <div>Halaman customers edit — dalam pengembangan</div>
    </>
  );
}
