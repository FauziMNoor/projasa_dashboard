import { CONFIG } from 'src/global-config';

// TODO: Implementasi halaman users edit

const metadata = { title: `users edit - ${CONFIG.appName}` };

export default function Page() {
  return (
    <>
      <title>{metadata.title}</title>
      <div>Halaman users edit — dalam pengembangan</div>
    </>
  );
}
