import { CONFIG } from 'src/global-config';

// TODO: Implementasi halaman portfolios list

const metadata = { title: `portfolios list - ${CONFIG.appName}` };

export default function Page() {
  return (
    <>
      <title>{metadata.title}</title>
      <div>Halaman portfolios list — dalam pengembangan</div>
    </>
  );
}
