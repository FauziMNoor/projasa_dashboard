import { CONFIG } from 'src/global-config';

// TODO: Implementasi halaman portfolios edit

const metadata = { title: `portfolios edit - ${CONFIG.appName}` };

export default function Page() {
  return (
    <>
      <title>{metadata.title}</title>
      <div>Halaman portfolios edit — dalam pengembangan</div>
    </>
  );
}
