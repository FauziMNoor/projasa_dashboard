import { CONFIG } from 'src/global-config';

// TODO: Implementasi halaman portfolios new

const metadata = { title: `portfolios new - ${CONFIG.appName}` };

export default function Page() {
  return (
    <>
      <title>{metadata.title}</title>
      <div>Halaman portfolios new — dalam pengembangan</div>
    </>
  );
}
