import { CONFIG } from 'src/global-config';

// TODO: Implementasi halaman dashboard finance

const metadata = { title: `dashboard finance - ${CONFIG.appName}` };

export default function Page() {
  return (
    <>
      <title>{metadata.title}</title>
      <div>Halaman dashboard finance — dalam pengembangan</div>
    </>
  );
}
