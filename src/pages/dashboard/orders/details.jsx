import { CONFIG } from 'src/global-config';

// TODO: Implementasi halaman orders details

const metadata = { title: `orders details - ${CONFIG.appName}` };

export default function Page() {
  return (
    <>
      <title>{metadata.title}</title>
      <div>Halaman orders details — dalam pengembangan</div>
    </>
  );
}
