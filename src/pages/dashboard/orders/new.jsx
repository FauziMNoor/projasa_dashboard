import { CONFIG } from 'src/global-config';

// TODO: Implementasi halaman orders new

const metadata = { title: `orders new - ${CONFIG.appName}` };

export default function Page() {
  return (
    <>
      <title>{metadata.title}</title>
      <div>Halaman orders new — dalam pengembangan</div>
    </>
  );
}
