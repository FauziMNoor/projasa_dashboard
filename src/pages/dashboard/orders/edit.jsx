import { CONFIG } from 'src/global-config';

// TODO: Implementasi halaman orders edit

const metadata = { title: `orders edit - ${CONFIG.appName}` };

export default function Page() {
  return (
    <>
      <title>{metadata.title}</title>
      <div>Halaman orders edit — dalam pengembangan</div>
    </>
  );
}
