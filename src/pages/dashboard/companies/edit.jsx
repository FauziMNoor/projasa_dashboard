import { CONFIG } from 'src/global-config';

// TODO: Implementasi halaman companies edit

const metadata = { title: `companies edit - ${CONFIG.appName}` };

export default function Page() {
  return (
    <>
      <title>{metadata.title}</title>
      <div>Halaman companies edit — dalam pengembangan</div>
    </>
  );
}
