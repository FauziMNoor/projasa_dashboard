import { CONFIG } from 'src/global-config';

// TODO: Implementasi halaman companies list

const metadata = { title: `companies list - ${CONFIG.appName}` };

export default function Page() {
  return (
    <>
      <title>{metadata.title}</title>
      <div>Halaman companies list — dalam pengembangan</div>
    </>
  );
}
