import { CONFIG } from 'src/global-config';

// TODO: Implementasi halaman companies new

const metadata = { title: `companies new - ${CONFIG.appName}` };

export default function Page() {
  return (
    <>
      <title>{metadata.title}</title>
      <div>Halaman companies new — dalam pengembangan</div>
    </>
  );
}
